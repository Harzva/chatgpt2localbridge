[package]
name = "chatgpt2localbridge-rs"
version = "0.1.0"
edition = "2024"
description = "Rust native local console and minimal MCP smoke server for ChatGPT2LocalBridge."
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
