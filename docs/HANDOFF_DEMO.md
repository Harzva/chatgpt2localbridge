# Handoff Demo

This document was created through `handoff.create -> codex.task_start` in local Codex Runner.

It proves that a structured handoff package can create a scoped artifact in the target repository.

Public-safety note: this demo intentionally contains no tokens, cookies, credentials, `.env` values, or private local absolute paths.
